#include <stdio.h>
#include <algorithm>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,i,num,res;
    while(cin>>n)
    {
        cin>>num;
        res=reverse begin.(num),end.(num);
        cout<<res<<endl;
    }
    return 0;
}
